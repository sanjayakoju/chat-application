
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chat-db?createDatabaseIfNotExist=true
    username: root
    password: Root@1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
    database-platform: mysql